# FastAPI and web framework
fastapi==0.136.1
uvicorn[standard]==0.27.0
websockets>=13,<16
python-multipart==0.0.6

# Configuration
pydantic>=2.10,<3
pydantic-settings==2.1.0
python-dotenv==1.0.0
pyyaml>=6.0.3,<7

# Data processing
pandas==2.1.4
numpy==1.26.3

# Scientific analysis (available in code tool)
scipy>=1.11,<2
statsmodels>=0.14,<0.15
scikit-learn>=1.3,<2
matplotlib>=3.7,<4

# Persistent IPython shell powering the code tool
ipython>=8.0,<9

# LLM providers
openai>=1.50,<3
anthropic>=0.97.0,<1
google-genai>=1.51,<2

# Optional: for vLLM compatibility (uses OpenAI client)
# vLLM is run separately, not installed as dependency
# Cron scheduling
croniter>=6.2.2

# HTTP Client
httpx==0.28.1

# Feishu / Lark gateway — only needed when FEISHU_GATEWAY_ENABLED=true.
# Keep comments on their own line: pyproject.toml reads this file verbatim as
# its dependency list, and a trailing "# ..." makes the line invalid PEP 508.
lark-oapi>=1.4.0
# Decrypts Feishu webhook payloads when an Encrypt Key is set in the console.
# The gateway falls back to pycryptodome (a hard dependency of lark-oapi), but
# relying on a transitive dep for this would break if lark-oapi ever drops it.
cryptography>=42.0

# WeChat (Weixin ClawBot / iLink) gateway — used by `python -m backend.weixin.qr_login`
# to render the login QR in the terminal. Pure Python, no system deps.
qrcode>=8.2,<9

# iOS in-app channel — APNs proactive push (optional — only when APNS_ENABLED=true)
aioapns>=3.0,<4
